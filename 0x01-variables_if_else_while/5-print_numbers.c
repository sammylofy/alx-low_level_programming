#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Code snippet to print 0-9
 *
 * Return: 0 (Success)
 *
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
		printf("%d", digit);
	printf("\n");
	return (0);
}
